# 빌드 산출물 (이미지 안에서 새로 빌드하므로 불필요)
target/

# 비밀키 — 이미지에 굽지 말 것 (환경변수로 주입)
src/main/resources/application-secret.properties

# IDE / 메타 파일
.settings/
.classpath
.project
.factorypath
.vscode/
HELP.md

# 로컬 업로드 파일
uploads/