<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.algolist.backend.problem.translation.TranslationDao">
	<resultMap id="TranslationMap" type="TranslatedProblemDto">
		<id column="problem_id" property="problemId"/>
		<result column="translated_title" property="translatedTitle"/>
		<result column="translated_description" property="translatedDescription"/>
	</resultMap>
	
	<resultMap id="OriginalMap" type="OriginalProblemDto">
		<id column="problem_id" property="problemId"/>
		<result column="title" property="originalTitle"/>
		<result column="description" property="originalDescription"/>
	</resultMap>

	<select id="getTranslatedProblem" resultMap="TranslationMap">
		SELECT problem_id, translated_title, translated_description
		FROM translated_problems
		WHERE problem_id = #{problemId}
	</select>

	<insert id="insertTranslatedProblem">
		INSERT INTO translated_problems (problem_id, translated_title, translated_description)
		VALUES (#{problemId}, #{translatedTitle}, #{translatedDescription})
	</insert>
	
	<select id="getOriginalProblem" resultMap="OriginalMap">
		SELECT problem_id, title, description
		FROM problems
		WHERE problem_id = #{problemId} AND site = 'CODEFORCES'
	</select>
</mapper>
