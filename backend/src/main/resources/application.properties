spring.application.name=backend

# DB 접속 설정
spring.datasource.url=jdbc:mysql://localhost:3306/algolist
spring.datasource.username=root
spring.datasource.password=ssafy
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# mapper 파일 경로 설정
mybatis.mapper-locations=classpath:mappers/*.xml

# 별칭 참조를 위한 패키지 지정
mybatis.type-aliases-package=com.algolist.backend.problem,com.algolist.backend.solution,com.algolist.backend.user.dto,com.algolist.backend.user.dto.response

# multipart 파일 업로드 제한
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=3MB

# 민감 정보는 application-secret.properties에서 관리 (.gitignore 적용)
spring.config.import=optional:classpㅇath:application-secret.properties


# <Spring Batch>
# 서버 시작 시 Job 자동 실행 방지
spring.batch.job.enabled=false
# Spring Batch 메타데이터 테이블 자동 생성 설정
spring.batch.jdbc.initialize-schema=always

# Batch 메타데이터 DB (비즈니스 DB랑 분리)
spring.datasource.batch-system.jdbc-url=jdbc:mysql://localhost:3306/batch_system?serverTimezone=UTC
spring.datasource.batch-system.username=root
spring.datasource.batch-system.password=ssafy
spring.datasource.batch-system.driver-class-name=com.mysql.cj.jdbc.Driver